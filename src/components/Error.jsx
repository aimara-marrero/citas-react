const Error = ({children}) => {
  return (
    <div className="bg-red-700 text-center uppercase text-white font-bold rounded-md p-3 mb-3">
     
     {children}
    </div>
  );
};

export default Error;
